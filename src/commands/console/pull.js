const {log} = require('../../logger.ts');

function pull() {
    log.warn('Currently being worked on');
}

function help() {
    log.warn('Currently being worked on');
}

module.exports = {pull, help}