const {log} = require('../../logger.ts');

function pull() {
    log.warning('Currently being worked on');
}

function help() {
    log.warning('Currently being worked on');
}

module.exports = {pull, help}